@model IEnumerable<TestWebAppCoolName.Models.Tag>

@{
    ViewBag.Title = "Admin Tag";
}

<main class="main">


    <p>
        @Html.ActionLink("Create New", "NewTag")
    </p>
    <table class="table">
        <tr>
            <th>
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "EditTag", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "DeleteTag", new { id = item.Id })
                </td>
            </tr>
        }

    </table>
</main>
